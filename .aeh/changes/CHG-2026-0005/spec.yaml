generated_at: '2026-08-29T04:35:12.604241+00:00'
requirements:
- acceptance:
  - id: AC-001-01
    statement: REFACTOR can enter SPEC_REPAIR or TEST_REPAIR only through the existing
      explicit repair conditions.
    type: automated
  - id: AC-001-02
    statement: REFACTOR or SPEC can return to GROUND only through an explicit GROUNDING_STALE
      repair condition.
    type: automated
  - id: AC-001-03
    statement: Repair never requires or permits direct editing of machine-owned Change
      state.
    type: invariant
  behavior: A governed Change with stale post-GREEN evidence can legally restart repair
    from REFACTOR.
  id: REQ-001
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-002-01
    statement: SCM_AUTHENTICATED_MERGE accepts only an APPROVED MERGE_GATE with a
      human actor and evidence reference, and verification reports READY_WITH_WARNINGS.
    type: automated
  - id: AC-002-02
    statement: The delegated mode cannot satisfy VERIFY_MANUAL or override any technical
      failure.
    type: invariant
  behavior: AEH supports an explicit SCM-authenticated MERGE_GATE delegation for solo
    repositories without requiring a local HMAC key.
  id: REQ-002
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-003-01
    statement: Existing HMAC approval and tamper tests continue to pass without changed
      call sites.
    type: automated
  - id: AC-003-02
    statement: Provider-neutral replay rejects SCM delegation unless a trusted adapter
      passes the exact accepted trust mode.
    type: automated
  - id: AC-003-03
    statement: Repository-controlled CI never receives an HMAC secret and delegated
      trust is never accepted implicitly.
    type: invariant
  behavior: Strict HMAC approval remains the default and provider-neutral replay remains
    fail-closed.
  id: REQ-003
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-004-01
    statement: Configured GitHub replay passes only the declared trust mode and reports
      merge_approval_channel plus an approval.channel check.
    type: automated
  - id: AC-004-02
    statement: The reported approval channel always matches the configured provider
      policy.
    type: invariant
  behavior: The GitHub adapter makes the trust downgrade visible and delegates final
    authority to the authenticated SCM merge action.
  id: REQ-004
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
