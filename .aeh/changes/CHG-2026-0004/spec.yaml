generated_at: '2026-09-01T22:07:46.321604+00:00'
requirements:
- acceptance:
  - id: AC-001-01
    statement: Unsupported network or cross-host state is rejected or explicitly outside
      the assurance claim.
    type: invariant
  behavior: Coordination remains a cooperating-process, single-host, local-filesystem
    protocol using standard-library locking, atomic file operations, and cryptographic
    hashing.
  id: REQ-001
  kind: CONSTRAINT
  source:
    refs:
    - M6_3_SPEC.md#2
    - M6_3_SPEC.md#5
    - M6_3_SPEC.md#6
    - M6_3_SPEC.md#11
    type: POLICY_CONSTRAINT
- acceptance:
  - id: AC-002-01
    statement: CHG-2026-0002 and CHG-2026-0003 credentials, lease tokens, and approvals
      are not reused for CHG-2026-0004.
    type: invariant
  behavior: M6.3C is delivered after merged M6.3B as a separate Change, branch, PR,
    approval chain, and rollback unit.
  id: REQ-002
  kind: CONSTRAINT
  source:
    refs:
    - M6_3_IMPLEMENTATION_PLAN.md#4
    - M6_3_IMPLEMENTATION_PLAN.md#7
    type: POLICY_CONSTRAINT
- acceptance:
  - id: AC-003-01
    statement: The v2 schema and exporter include protocol version, coordination state,
      repository hash, workspace hash, lease revision, last accepted truth hash, last
      receipt digest, and optional external workspace-ref hash from one stable snapshot.
    type: automated
  - id: AC-003-02
    statement: AEW export contains no raw token, token-file path, repository identifier,
      workspace path, state-root path, credential, or secret body; identical accepted
      inputs produce byte-equivalent canonical output.
    type: invariant
  behavior: AEW governance export version 2 deterministically binds the exported artifact
    set to non-secret coordination provenance.
  id: REQ-003
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-004-01
    statement: Replay succeeds from one accepted generation and returns a defined
      non-success verdict when a writer overlaps, an operation is unresolved, or Change
      truth changes during the read.
    type: automated
  - id: AC-004-02
    statement: CI replay cannot combine artifacts from different Change generations
      and cannot activate coordination for a legacy Change.
    type: invariant
  behavior: CI replay consumes Change artifacts through the same stable snapshot contract
    without weakening its existing read-only, no-project-code, and exact-head assurances.
  id: REQ-004
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-005-01
    statement: Status returns stable data for coordinated Changes, reports NOT_ACTIVATED
      for never-coordinated legacy Changes, and fails closed during overlapping mutation
      or unresolved truth drift.
    type: automated
  - id: AC-005-02
    statement: Status does not create or mutate the external store and never exposes
      a token, token-file path, raw state-root path, or raw workspace path.
    type: invariant
  behavior: Change status remains token-free and read-only while reporting only a
    stable Change generation with non-secret coordination provenance.
  id: REQ-005
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-006-01
    statement: Concurrent readers coexist, writer begin/finalize is excluded while
      a snapshot is active, and every successful snapshot has identical pre-read and
      post-read truth hashes.
    type: automated
  - id: AC-006-02
    statement: An active operation, accepted-truth mismatch, lock timeout, malformed
      store, unsupported version, or artifact drift returns a deterministic blocked
      or inconclusive result and never mixed generations.
    type: invariant
  behavior: Change-scoped readers use one bounded shared-lock snapshot primitive that
    validates coordination state and Change truth before and after reading artifacts.
  id: REQ-006
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-007-01
    statement: Identical receipt bodies produce identical digests, token canaries
      are absent from every prohibited sink, and two Changes in distinct workspaces
      cannot write into each other's Change or declared output artifacts.
    type: automated
  - id: AC-007-02
    statement: No failure message, receipt, store document, test artifact, repository
      byte, normal output, or AEW envelope reveals raw lease authority.
    type: invariant
  behavior: Coordination receipts and cross-workspace artifacts remain deterministic,
    schema-valid, redacted, and isolated under accepted, rejected, recovery, and fault
    paths.
  id: REQ-007
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-008-01
    statement: Documentation checks pass and required current documents cover protocol
      limits, lease lifecycle, stable reads, AEW binding, recovery, drain, rollback,
      and failure codes.
    type: automated
  - id: AC-008-02
    statement: Frozen release and archive evidence remain byte-unchanged and the documentation
      does not claim cross-host, network-filesystem, administrator-proof, or scheduler
      guarantees.
    type: invariant
  behavior: Current architecture, roadmap, status, decisions, engineering guide, documentation
    contract, README, and changelog describe exactly the achieved M6.3 coordination
    boundary and manual operating procedure.
  id: REQ-008
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-009-01
    statement: Focused stable-reader, AEW, multi-process, redaction, and crossover
      suites plus full regression, docs check, wheel build, clean-room install, doctor,
      repair, upgrade/rollback, M4/M5, and CI replay pass.
    type: automated
  - id: AC-009-02
    statement: The workflow retains bounded execution while allowing the supported
      full regression to complete on the slowest observed matrix leg and introduces
      no new third-party runtime dependency.
    type: invariant
  behavior: M6.3C remains independently reviewable and completes the supported Windows
    and Ubuntu, Python 3.10 and 3.11 assurance matrix without an undersized workflow
    timeout.
  id: REQ-009
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
- acceptance:
  - id: AC-010-01
    statement: Supported-platform tests use independent processes rather than thread-only
      substitutes and prove unique reservations, exclusive writers, shared readers,
      crash release of OS locks, retained logical lease blocking, and deterministic
      recovery serialization.
    type: automated
  - id: AC-010-02
    statement: Fault injection accepts only complete old state or complete new state;
      torn state, clock rollback, unsafe aliases, and unresolved operations fail closed.
    type: invariant
  behavior: Real multi-process workers exercise barriers, process termination, lock
    contention, reader/writer overlap, reservation races, recovery races, and partial-write
    boundaries on Windows and Ubuntu.
  id: REQ-010
  kind: DESIRED
  source:
    refs: []
    type: USER_REQUIREMENT
scope:
  in:
  - stable_change_read_snapshots
  - change_status_snapshot_binding
  - ci_replay_snapshot_binding
  - aew_v2_coordination_provenance
  - deterministic_redacted_receipts
  - real_multiprocess_fault_matrix
  - artifact_crossover_guards
  - current_documentation_reconciliation
  - supported_ci_matrix_reliability
  out:
  - agent_scheduling_or_task_decomposition
  - autonomous_lease_renewal_or_recovery_daemons
  - cross_host_or_network_filesystem_coordination
  - branch_protection_or_scm_admin_mutation
  - release_tag_or_pypi_publication
  - frozen_release_or_archive_evidence_rewrites
